
// TODO: real implementation
export const useGeocodedLocations = (locations: any[], mapboxToken: string | null) => ({
  geocodedLocations: [],
  isLoading: false,
  error: null,
  retryGeocode: () => {},
});
